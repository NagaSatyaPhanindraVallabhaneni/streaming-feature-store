services:
  streaming-feature-store:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Persist the SQLite online store across restarts.
      - ./data:/app/data
    environment:
      - PYTHONPATH=/app/src
      - STORE_BACKEND=sqlite
      - SQLITE_PATH=/app/data/feature_store.db
